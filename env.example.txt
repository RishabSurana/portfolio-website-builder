# ===================================
# Contentstack Configuration
# ===================================

# Stack API Key (found in Stack Settings > API Credentials)
CONTENTSTACK_API_KEY=your_api_key_here

# Delivery Token (for fetching content)
CONTENTSTACK_DELIVERY_TOKEN=your_delivery_token_here

# Management Token (for creating/updating content)
# Generate in Stack Settings > Tokens > Management Tokens
CONTENTSTACK_MANAGEMENT_TOKEN=your_management_token_here

# Environment name (e.g., 'development', 'production')
CONTENTSTACK_ENVIRONMENT=development

# Region: 'us', 'eu', 'azure-na', 'azure-eu'
CONTENTSTACK_REGION=us

# Branch (optional, defaults to 'main')
CONTENTSTACK_BRANCH=main

# ===================================
# Contentstack Launch Configuration
# ===================================

# Launch Project ID (found in Launch project settings)
CONTENTSTACK_LAUNCH_PROJECT_ID=your_launch_project_id

# Launch API Token (if using API triggers)
CONTENTSTACK_LAUNCH_TOKEN=your_launch_token

# Launch API Endpoint (usually not needed to change)
CONTENTSTACK_LAUNCH_API_ENDPOINT=https://launch-api.contentstack.com

# ===================================
# AI Configuration
# ===================================

# Choose AI provider: 'openai' or 'groq'
AI_PROVIDER=groq

# OpenAI API Key (from platform.openai.com)
# Required if AI_PROVIDER=openai
OPENAI_API_KEY=sk-your_openai_api_key_here

# GROQ API Key (from console.groq.com)
# Required if AI_PROVIDER=groq
# GROQ offers faster inference with Llama 3.3, Mixtral models
GROQ_API_KEY=gsk_your_groq_api_key_here

# GROQ Model (optional, defaults to llama-3.3-70b-versatile)
# Options: llama-3.3-70b-versatile, llama-3.1-70b-versatile, llama-3.1-8b-instant, mixtral-8x7b-32768, gemma2-9b-it
GROQ_MODEL=llama-3.3-70b-versatile

# ===================================
# GitHub Configuration (Optional)
# ===================================

# GitHub Personal Access Token (for higher rate limits)
# Create at: github.com/settings/tokens
# Scopes needed: public_repo (read-only access to public repos)
GITHUB_TOKEN=ghp_your_github_token_here

# ===================================
# Deployment Configuration
# ===================================

# Custom webhook URL for deployment triggers (optional)
DEPLOYMENT_WEBHOOK_URL=

# Webhook secret for authentication (optional)
WEBHOOK_SECRET=

# Base URL for generated portfolios
PORTFOLIO_BASE_URL=https://portfolios.contentstack.app

